from gaussian_splatting.config import ExperimentConfig


def train(config: ExperimentConfig) -> None:
    """Optimize a Gaussian scene from registered images."""
    # TODO(student): connect data, model, renderer, loss, optimizer, and density control.
    raise NotImplementedError("Milestone 3: implement the training loop")

